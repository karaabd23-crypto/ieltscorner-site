# SEO Report — Week 2026-30 (2026-07-20 to 2026-07-26)

Sources: `cro/snapshots/gsc-2026-30.json` (generated 2026-07-27) vs
`cro/snapshots/gsc-2026-29.json` (2026-07-13 to 2026-07-19, generated
2026-07-25); `cro/snapshots/2026-30.json` vs `cro/snapshots/2026-29.json` (GA4).

## 1. Indexing health

**Still not recovered.** The fix (commit `36bc457`, "Fix indexing crash: dedupe
lessons, prune sitemap, add Search Console SEO spine") merged 2026-07-25.

| | Week 29 (pre-fix, dl 2026-07-19) | Week 30 (post-fix, dl 2026-07-26) |
|---|---|---|
| Sitemap submitted | 579 | 477 |
| Sitemap indexed | 0 | 0 |

- Submitted URLs dropped 579 → 477 (−102), consistent with the lesson dedupe
  removing duplicate URLs from the sitemap — the fix is reflected in the
  submitted count.
- **Indexed is still 0** in the most recent GSC pull (`sitemap-index.xml`,
  downloaded 2026-07-26, one day after the fix shipped). Note the
  `www.ieltscorner.ca` sitemap variant's `lastDownloaded` is still
  2026-07-19 — Google hasn't recrawled that host variant since before the
  fix, so it isn't informative yet.
- One day of post-fix crawl data is too early to call this a failure — Google
  typically takes days to weeks to re-index after a sitemap change. But there
  is **no evidence of recovery yet** in the data on hand. Total impressions
  also fell 123 → 79 week-over-week, and total clicks were 0 → 3, position
  22.4 → 11.9 — small movement, not yet a recovery signal at this scale.
- **Action:** keep watching next week's `sitemaps[]` entry for `indexed > 0`.
  If it's still 0 after 2-3 more weekly pulls, request re-indexing via GSC's
  URL Inspection tool for a handful of priority pages and verify the sitemap
  itself is reachable and valid (no submission/format errors reported so
  far — `errors: 0, warnings: 0` both weeks).

## 2. Fastest ranking wins — page-2 keywords

Only one query in the position 8-20 band across both weeks:

| Query | Week 29 | Week 30 |
|---|---|---|
| "praxis" | pos 9.6, 42 impr, 0 clicks | pos 8.7, 28 impr, 0 clicks |

Position improved slightly (9.6 → 8.7, now bordering page 1) but impressions
fell (42 → 28) and clicks remain 0 in both weeks.

The snapshot's `topQueries`/`topPages` are separate aggregates with no
query-to-page join, so I can't say which page ranks for "praxis" without
inventing it — the GSC snapshot schema doesn't include that mapping. Also
worth flagging: "praxis"/"praxsis" doesn't obviously match IELTS/CELPIP
intent — worth checking in GSC's UI (query → page drill-down) whether this
term is even relevant to the site's target keywords before investing effort
matching a title/H1 to it.

**Action:** in the GSC dashboard (not available in this snapshot), look up
which page "praxis" ranks for, confirm the search intent is relevant, and
only then decide whether a title/H1 tweak is worth it.

## 3. Impressions but no clicks — title/meta problems

Same single entry: "praxis" — 28 impressions, position 8.7, 0% CTR (week 30).
No other queries appear in `impressionsNoClicks[]` either week. With overall
impressions this low (79 total site-wide in week 30), this section will stay
thin until indexing recovers and more pages start generating impressions.

## 4. Traffic & conversion trend (GA4)

| Metric | Week 29 | Week 30 | Change |
|---|---|---|---|
| Pageviews | 1,354 | 1,059 | −22% |
| Visitors | 845 | 344 | −59% |
| Bounce rate | 89.8% | 71.4% | −18.4pp (better) |
| Avg. duration | 14.7s | 40.6s | +176% (better) |

Visitor volume dropped sharply, but engagement quality improved a lot
(lower bounce, ~3x longer sessions) — consistent with less low-intent/bot
traffic rather than a genuine demand drop. Since organic clicks are only 0-3
per week right now, GA4 volume is being driven almost entirely by non-search
channels — this traffic swing is not an indexing signal either way.

Conversion goals (`conversions[].rate`, joined to `cro/conversions.json`):

| Goal | Week 29 (visits/events/rate) | Week 30 (visits/events/rate) |
|---|---|---|
| Ebook checkout click (revenue) | 12 / 0 / 0% | 16 / 0 / 0% |
| Private tutoring booking (revenue) | 4 / 0 / 0% | 5 / 1 / **20%** |
| AI feedback checkout click (revenue) | 10 / 0 / 0% | 46 / 0 / 0% |
| Subscription checkout click (revenue) | 1 / 0 / 0% | 33 / 0 / 0% |
| Webinar signup | 0 / 0 / 0% | *(not in week-30 snapshot)* |

Flag: AI feedback and Subscription checkout-click visits rose sharply
(10→46, 1→33) but conversion stayed at 0% both weeks — a real funnel gap on
two revenue goals, independent of the indexing issue. Tutoring is the only
goal with any recorded conversion (1 event, 20% rate) — small sample, watch
next week before reading into it.

## 5. This week's 3 actions

1. **Verify the indexing fix is taking hold — check GSC directly, don't wait
   for the next snapshot.** One day of post-fix crawl data shows indexed
   still at 0/477. Use GSC's URL Inspection tool on 3-5 priority pages (e.g.
   `/`, `/celpip/reading/`, `/ai-feedback/`) to confirm they're crawlable and
   request indexing if not already queued. This is still the #1 lever until
   sitemap `indexed` moves off 0.
2. **Investigate the AI-feedback and subscription checkout funnels.** Visits
   to both jumped this week (46 and 33) with 0 conversions on either —
   before writing this off as an SEO problem, check the checkout flow itself
   (button behavior, page errors, event tracking) since this is a conversion
   gap, not a traffic gap.
3. **Backlinks remain the medium-term gap** once indexing recovers and page-2
   keywords have something to rank on — see `BACKLINK_OUTREACH_KIT.md`. Not
   repeating detail here; revisit once `indexed > 0` is confirmed.
