# SEO Report — Week 2026-31 (2026-07-27 to 2026-08-02)

Sources: `cro/snapshots/gsc-2026-31.json` (generated 2026-08-03) vs
`cro/snapshots/gsc-2026-30.json` (2026-07-20 to 2026-07-26, generated
2026-07-27); `cro/snapshots/2026-31.json` vs `cro/snapshots/2026-30.json`
(GA4).

## 1. Indexing health

**Still not recovered — and the "too early to tell" window has closed.**
The fix (commit `36bc457`, "Fix indexing crash: dedupe lessons, prune
sitemap, add Search Console SEO spine") merged 2026-07-25.

| | Week 30 (post-fix, dl 2026-07-26) | Week 31 (post-fix, dl 2026-07-31) |
|---|---|---|
| Sitemap submitted (`sitemap-index.xml`) | 477 | 477 |
| Sitemap indexed | 0 | 0 |
| `www.` variant submitted / indexed / dl | 477 / 0 / 2026-07-19 (stale) | 477 / 0 / 2026-07-29 |

- Both sitemap variants have now been recrawled well after the fix — the
  primary `sitemap-index.xml` 6 days post-fix, the `www.` variant 4 days
  post-fix — and **both still report 0 indexed**. Last week's report noted
  one day of post-fix data was too early to judge; that's no longer the
  case. `errors: 0, warnings: 0` on both, so this isn't a submission/format
  problem — Google simply hasn't indexed the sitemap URLs yet.
- Caveat worth flagging: individual pages *are* generating impressions in
  `topPages[]` this week — homepage, `/about/`, `/ai-feedback/`,
  `/celpip/reading/`, several `/lessons/...` and `/blog/...` URLs, even
  `/questions/...` pages. So Google has crawled and is surfacing some pages
  in search results despite the sitemap report's `indexed` count reading 0.
  I can't reconcile that discrepancy from this snapshot alone — the
  sitemap-coverage stat and page-level impressions may lag each other or
  measure different things. This is worth checking directly in GSC's **Page
  indexing** (Coverage) report, not just the sitemap stat, before concluding
  nothing is indexed at all.
- Total impressions rose 79 → 121 (+53%) and clicks held at 3, but average
  position worsened slightly (11.9 → 13.3).

**Action:** don't wait on another weekly pull. Check GSC's Page Indexing
report directly for the actual count and reasons (crawled-not-indexed,
discovered-not-indexed, etc.), and use URL Inspection to request indexing
on a handful of priority pages if they're still not queued.

## 2. Fastest ranking wins — page-2 keywords

Still only one query in the position 8-20 band:

| Query | Week 30 | Week 31 |
|---|---|---|
| "praxis" | pos 8.7, 28 impr, 0 clicks | pos 9.8, 37 impr, 0 clicks |

Impressions grew (+32%) but position slipped back (8.7 → 9.8) and CTR is
still 0% two weeks running. As noted last week, "praxis" doesn't obviously
match IELTS/CELPIP search intent — the snapshot has no query-to-page join,
so I can't say which page ranks for it without inventing that. Worth a
manual look in the GSC UI's query→page drill-down before spending effort
tightening a title/H1 to match this term.

New this week (not page-2 band, but notable): several CELPIP-specific
queries now show up in `topQueries[]` that weren't present last week
("celpip reading test" pos 18, "celpip express entry canada" pos 41,
"chatgpt celpip writing tool" pos 19, etc.), suggesting query diversity is
increasing even though positions are mostly weak (page 3+). Two branded
queries — "education consultant" (pos 1, 2/5 clicks) and "education
consulting" (pos 1, 1/2 clicks) — account for all 3 of this week's clicks.

## 3. Impressions but no clicks — title/meta problems

Same single entry both weeks: "praxis" (37 impressions, position 9.8, 0%
CTR this week). No other queries populate `impressionsNoClicks[]` — this
section stays thin until indexing recovers and more queries generate
impressions.

Separately, in `topPages[]` the non-canonical `http://ieltscorner.ca/`
(no `s`) shows 59 impressions and all 3 of this week's clicks at position 8,
while `https://ieltscorner.ca/` shows 15 impressions, 0 clicks, and a much
worse position (22.9) — Google appears to be tracking these as two distinct
URLs. Worth verifying the http→https redirect/canonical setup in Netlify's
domain settings; if the insecure URL is what's actually ranking, that's a
duplicate-URL issue in the same family as the dedupe fix already shipped,
just not yet fully closed.

## 4. Traffic & conversion trend (GA4)

| Metric | Week 30 | Week 31 | Change |
|---|---|---|---|
| Pageviews | 1,059 | 1,171 | +10.6% |
| Visitors | 344 | 419 | +21.8% |
| Bounce rate | 71.4% | 71.4% | flat |
| Avg. duration | 40.6s | 24.8s | −39% (worse) |

Visitor volume is up, but average session duration dropped sharply while
bounce rate held flat — engagement quality moved the wrong direction even
as raw traffic grew. With organic clicks still only 3/week, this GA4 shift
is being driven by non-search channels, not an indexing signal.

Conversion goals (`conversions[].rate`, joined to `cro/conversions.json`):

| Goal | Week 30 (visits/events/rate) | Week 31 (visits/events/rate) |
|---|---|---|
| Ebook checkout click (revenue) | 16 / 0 / 0% | 18 / 0 / 0% |
| Private tutoring booking (revenue) | 5 / 1 / 20% | 19 / 0 / **0%** |
| AI feedback checkout click (revenue) | 46 / 0 / 0% | 38 / 0 / 0% |
| Subscription checkout click (revenue) | 33 / 0 / 0% | 29 / 0 / 0% |

**Flag:** private tutoring booking rate dropped from 20% to 0% — visits
nearly quadrupled (5 → 19) but produced zero conversions, versus one
conversion last week on a fifth of the traffic. Worth checking the booking
flow/event tracking for a regression rather than assuming it's a demand
issue. Ebook, AI feedback, and subscription checkout clicks remain at 0%
conversion for a second straight week each.

## 5. This week's 3 actions

1. **Check GSC's Page Indexing report directly, don't wait on next week's
   pull.** The sitemap stat has read 0 indexed for two consecutive post-fix
   crawls (6 and 4 days out) with no submission errors, yet individual pages
   are showing up in search impressions — that discrepancy needs the GSC UI
   to resolve, not another snapshot. Use URL Inspection to request indexing
   on 3-5 priority pages (`/`, `/celpip/reading/`, `/ai-feedback/`) if
   they're not already queued.
2. **Investigate the private-tutoring booking funnel.** Visits nearly
   quadrupled (5 → 19) while conversions dropped from 1 to 0 — check the
   booking flow and event tracking before assuming this is a traffic-quality
   problem.
3. **Verify the http→https redirect/canonical setup for the homepage.** GSC
   is crediting all 3 of this week's clicks and the better position (8 vs
   22.9) to the insecure `http://ieltscorner.ca/` URL instead of the
   canonical `https://` version — confirm Netlify's domain/TLS redirect is
   actually forcing HTTPS, since this is the same class of duplicate-URL
   issue the dedupe fix targeted.
