# SEO Report — Week 2026-33 (2026-08-10 to 2026-08-16)

Sources: `cro/snapshots/gsc-2026-33.json` (generated 2026-08-16) vs
`cro/snapshots/gsc-2026-32.json` (2026-08-03 to 2026-08-09, generated
2026-08-10). GA4 snapshot `cro/snapshots/2026-33.json` vs the last available
prior GA4 snapshot, `cro/snapshots/2026-31.json` (2026-07-27 to 2026-08-02) —
**no `2026-32.json` GA4 snapshot exists**, so the GA4 comparison spans two
weeks, not one.

## 1. Indexing health

**Still 0 indexed — four consecutive post-fix weekly pulls now.** The fix
("Fix indexing crash: dedupe lessons, prune sitemap, add Search Console SEO
spine", commit `36bc457`) merged 2026-07-25, 22 days before this snapshot.

| | Week 30 (dl 07-26) | Week 31 (dl 07-31) | Week 32 (dl 08-09) | Week 33 (dl 08-16) |
|---|---|---|---|---|
| `sitemap-index.xml` submitted | 477 | 477 | 477 | 477 |
| `sitemap-index.xml` indexed | 0 | 0 | 0 | 0 |
| `www.` variant submitted | 477 | 477 | 477 | 477 |
| `www.` variant indexed | 0 | 0 | 0 | 0 |
| `www.` variant last recrawled | 07-19 | 07-29 | 08-09 | 08-09 (stale — unchanged) |
| Errors / warnings | 0 / 0 | 0 / 0 | 0 / 0 | 0 / 0 |

- Both sitemap variants have now been checked four times since the fix
  shipped and both still report **0 indexed**, with no submission errors or
  warnings. This is no longer a "give it more time" situation.
- I checked the live repo for a code-side cause and found none: `public/robots.txt`
  allows `User-agent: *` with no blanket disallow (only `/admin/` and
  `/book/paid/` are blocked, both correctly), and `noindex` in
  `src/layouts/Layout.astro` / `ConversionLayout.astro` defaults to `false`
  — every `noindex={true}` usage I found is on thank-you/checkout/success
  pages, which is correct practice, not a stray global flag. So the block is
  not something visible in the codebase; it has to be checked in Search
  Console directly (Page Indexing report, URL Inspection).
- `topPages[]` again shows real impressions this week (61 for the homepage,
  17 for `/blog/pte-core-listening-guide/`, 12 for `/ai-feedback/`, several
  `/blog/...` and `/lessons/...` URLs) even though the sitemap-coverage stat
  reads 0 indexed — same discrepancy flagged the last two weeks, still
  unreconciled from snapshot data alone.
- The canonical/non-canonical homepage split got **worse**, not better:
  `http://ieltscorner.ca/` (non-canonical) had 61 impressions at position
  7.7, while `https://ieltscorner.ca/` (canonical) had only 10 impressions
  at position 16.2 — a wider gap than week 32's 37-vs-11 / 8.2-vs-10.5 split.
  Neither got a click. This is the second week running the gap widened
  instead of closing; worth verifying the HTTPS redirect/canonical tag is
  actually forcing the `https://` (non-`www`) host in Netlify.
- Overall search totals: clicks flat at 1, impressions up 106 → 164 (+55%),
  average position improved slightly 15.2 → 14.2. Impressions rising while
  indexed stays 0 is consistent with pages being crawled/discovered but not
  fully indexed — another reason to check the Page Indexing report's
  crawled-not-indexed / discovered-not-indexed breakdown directly.

## 2. Fastest ranking wins — page-2 keywords

Still only one query in the position 8-20 band, four weeks running:

| Query | Week 30 | Week 31 | Week 32 | Week 33 |
|---|---|---|---|---|
| "praxis" | pos 8.7, 28 impr | pos 9.8, 37 impr | pos 9.5, 26 impr | pos 8.3, 49 impr |

This is the week's best-moving signal: position improved to 8.3 (its best
in four weeks, right at the page-1/page-2 boundary) and impressions nearly
doubled (26 → 49). Still 0 clicks in every week so far, though.

As in prior weeks: the snapshot has no query-to-page join, so I can't say
which page ranks for "praxis" without inventing it, and the term still
doesn't obviously match IELTS/CELPIP search intent. Before investing effort
tightening a title/H1 to it, confirm in the GSC UI's query→page drill-down
which page it's landing on and whether the intent is actually relevant.

`topQueries[]` keeps showing more CELPIP/visa-related long-tail terms
("celpip express entry canada", "celpip for federal skilled worker",
"celpip skilled independent visa", "celpip reading pro") — all at weak
positions (26-74) and 0 clicks. Query diversity keeps growing but nothing
besides "praxis" has crossed into the page-2 band yet.

## 3. Impressions but no clicks — title/meta problems

Same single entry, four weeks running: "praxis" (49 impressions this week,
position 8.3, 0% CTR). No other queries populate `impressionsNoClicks[]` —
this section will stay thin until indexing recovers and more queries start
generating impressions site-wide.

## 4. Traffic & conversion trend (GA4)

Comparing week 33 to week 31 (the last available GA4 snapshot — **week 32's
GA4 snapshot is missing**, so this is a two-week-apart comparison, not a
clean week-over-week one):

| Metric | Week 31 | Week 33 | Change |
|---|---|---|---|
| Pageviews | 1,171 | 499 | −57.4% |
| Visitors | 419 | 371 | −11.5% |
| Bounce rate | 71.4% | 82.0% | +10.6pp (worse) |
| Avg. duration | 24.8s | 40.7s | +64.5% (better) |

| Goal | Week 31 visits → events (rate) | Week 33 visits → events (rate) |
|---|---|---|
| Ebook checkout click | 18 → 0 (0%) | 2 → 0 (0%) |
| Private tutoring booking | 19 → 0 (0%) | 3 → 0 (0%) |
| AI feedback checkout click | 38 → 0 (0%) | 4 → 0 (0%) |
| Subscription checkout click | 29 → 0 (0%) | 21 → 0 (0%) |

Traffic volume and engagement moved in opposite directions again (fewer
pageviews and visitors, but longer sessions and — this week — a worse bounce
rate). With no GA4 pull for week 32, I can't tell whether this was a gradual
slide or a single bad week. The private-tutoring conversion rate drop
flagged in the week-31 and week-32 reports (20% → 0%) is still unresolved:
every tracked goal shows 0 events in both the week-31 and week-33 snapshots.
Organic clicks are still only 1-3/week, so this GA4 movement is not an
indexing signal — it's non-search traffic, and secondary until indexing
recovers.

## 5. This week's 3 actions

1. **Escalate the indexing check into Search Console itself — the automated
   snapshot alone can't resolve this anymore.** Four consecutive post-fix
   pulls (07-26, 07-31, 08-09, 08-16) all show 0 indexed on both sitemap
   variants with zero submission errors, and the live repo shows no
   code-side block (robots.txt is open, no stray global `noindex`). Pull up
   GSC's **Page Indexing (Coverage)** report directly to see the real reason
   codes (crawled-not-indexed vs. discovered-not-indexed vs. something else),
   and use **URL Inspection → Request Indexing** on 3-5 priority pages (`/`,
   `/celpip/reading/`, `/ai-feedback/`) if they aren't already queued.
2. **Confirm the HTTPS canonical redirect in Netlify.** The non-canonical
   `http://ieltscorner.ca/` is still outranking the canonical
   `https://ieltscorner.ca/` in impressions and position, and the gap widened
   again this week (7.7 vs. 16.2). Verify the domain/TLS redirect forces
   `https://ieltscorner.ca` site-wide so Google consolidates signals onto
   the canonical URL.
3. **Get a GA4 pull for week 32 (or confirm why it's missing) and re-check
   the private tutoring booking funnel.** The 20% → 0% conversion-rate drop
   flagged two reports ago is still open with 0 events in every subsequent
   snapshot — a missing week makes it hard to tell if this is a lasting
   regression. Backlinks remain the underlying gap for organic growth once
   indexing recovers — see `BACKLINK_OUTREACH_KIT.md`, not repeating the
   detail here.
