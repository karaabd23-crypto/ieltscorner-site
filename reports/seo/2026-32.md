# SEO Report — Week 2026-32 (2026-08-03 to 2026-08-09)

Sources: `cro/snapshots/gsc-2026-32.json` (generated 2026-08-10) vs
`cro/snapshots/gsc-2026-31.json` (2026-07-27 to 2026-08-02, generated
2026-08-03). GA4 snapshot `cro/snapshots/2026-31.json` (2026-07-27 to
2026-08-02) is still the latest available — no `2026-32.json` GA4 snapshot
exists yet, so section 4 has no new data this week.

## 1. Indexing health

**Still 0 indexed — three consecutive post-fix weeks now.** The fix
("Fix indexing crash: dedupe lessons, prune sitemap, add Search Console
SEO spine") merged 2026-07-25.

| | Week 30 (dl 07-26) | Week 31 (dl 07-31) | Week 32 (dl 08-09) |
|---|---|---|---|
| `sitemap-index.xml` submitted | 477 | 477 | 477 |
| `sitemap-index.xml` indexed | 0 | 0 | 0 |
| `www.` variant submitted | 477 | 477 | 477 |
| `www.` variant indexed | 0 | 0 | 0 |
| Errors / warnings | 0 / 0 | 0 / 0 | 0 / 0 |

- Both sitemap variants have now been recrawled three times since the fix
  shipped (15 days ago) and both still report **0 indexed**, with no
  submission errors or warnings. This has moved past "give it more time" —
  the automated snapshot alone isn't going to resolve this; it needs a
  direct look in GSC's Page Indexing (Coverage) report and URL Inspection
  on priority pages, as flagged last week.
- As last week, `topPages[]` still shows real impressions this week
  (homepage, `/about/`, `/ai-feedback/`, `/celpip/reading/`, several
  `/lessons/...` URLs) even though the sitemap-coverage stat reads 0
  indexed — I can't reconcile that from this snapshot alone; it's the same
  discrepancy noted last week and still needs checking directly in the GSC
  UI rather than assumed away.
- Overall search totals moved the wrong direction week over week: clicks
  3 → 1, impressions 121 → 106 (−12%), average position 13.3 → 15.2
  (worse). This is consistent with indexing not recovering, not a new
  signal on its own.
- The http/https duplicate-homepage issue flagged last week has narrowed
  but not resolved: `http://ieltscorner.ca/` still shows more impressions
  and clicks (37 impr, 1 click, position 8.2) than the canonical
  `https://ieltscorner.ca/` (11 impr, 0 clicks, position 10.5), though the
  gap is smaller than last week's 59-vs-15 / position-8-vs-22.9 split.
  Still worth confirming the HTTPS redirect/canonical setup in Netlify.

## 2. Fastest ranking wins — page-2 keywords

Still only one query in the position 8-20 band, three weeks running:

| Query | Week 30 | Week 31 | Week 32 |
|---|---|---|---|
| "praxis" | pos 8.7, 28 impr, 0 clicks | pos 9.8, 37 impr, 0 clicks | pos 9.5, 26 impr, 0 clicks |

Position ticked slightly better (9.8 → 9.5) but impressions fell (37 → 26)
and CTR is still 0% for a third straight week. As noted previously, "praxis"
doesn't obviously match IELTS/CELPIP intent and the snapshot has no
query-to-page join, so I can't say which page ranks for it without
inventing that — worth a manual check in the GSC UI's query→page
drill-down before spending effort on a title/H1 change here.

`topQueries[]` shows more CELPIP-specific terms surfacing this week
("celpip clb 9 crs points", "celpip reading tests", "celpip ircc
accepted", "celpip for federal skilled trades/worker") — all at weak
positions (28-48) and 0 clicks, so query diversity keeps growing but
nothing has crossed into a rankable band besides "praxis" yet.

## 3. Impressions but no clicks — title/meta problems

Same single entry three weeks running: "praxis" (26 impressions this
week, position 9.5, 0% CTR). No other queries populate
`impressionsNoClicks[]` — this section stays thin until indexing recovers
and more queries start generating impressions.

## 4. Traffic & conversion trend (GA4)

No new GA4 snapshot this week — `cro/snapshots/2026-31.json` (range
2026-07-27 to 2026-08-02) is still the latest. Per last week's report:
visitors 344 → 419 (+21.8%), pageviews 1,059 → 1,171 (+10.6%), bounce rate
flat at 71.4%, avg. duration down 40.6s → 24.8s. Private tutoring booking
conversion rate dropped 20% → 0% (visits 5 → 19, events 1 → 0) — that flag
is still open and unconfirmed one way or the other since no fresher GA4
data has landed. With organic clicks at 1-3/week, GA4 traffic continues to
be driven almost entirely by non-search channels, not an indexing signal.

## 5. This week's 3 actions

1. **Escalate the indexing investigation beyond the automated snapshot.**
   Three consecutive post-fix weekly pulls (07-26, 07-31, 08-09) all show
   0 indexed on both sitemap variants with zero submission errors, while
   individual pages still generate search impressions. Check GSC's Page
   Indexing report directly for the real count and reasons
   (crawled-not-indexed, discovered-not-indexed, etc.) and use URL
   Inspection to request indexing on 3-5 priority pages (`/`,
   `/celpip/reading/`, `/ai-feedback/`) if they're not already queued.
2. **Confirm the HTTPS redirect/canonical setup for the homepage.** The
   non-canonical `http://` URL is still outperforming the canonical
   `https://` URL in both impressions and clicks (gap narrowed vs. last
   week but not closed) — verify Netlify's domain/TLS redirect is forcing
   HTTPS site-wide.
3. **Re-check the private tutoring booking funnel once a fresher GA4
   snapshot lands.** Last week's report flagged a rate drop from 20% to
   0% on a nearly 4x visit increase; no new GA4 data this week to confirm
   whether that's a lasting regression or a one-week blip, so this stays
   open rather than resolved. Backlinks remain the underlying gap for
   organic growth once indexing recovers — see `BACKLINK_OUTREACH_KIT.md`,
   not repeating the detail here.
